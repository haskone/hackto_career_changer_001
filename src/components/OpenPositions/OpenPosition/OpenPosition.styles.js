import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({
  root: {
    margin: 10,
  },
  positionTitle: {
    marginBottom: 12
  },
  sectionRoot: {
    textAlign: 'left'
  },
  descriptionItem: {
    marginTop: 12
  },
  positionDescription: {
    marginTop: 24
  },
  seeJourneyButton: {
    marginTop: 16
  }
}));

export {useStyles};
